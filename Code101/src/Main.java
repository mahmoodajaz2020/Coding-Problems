import leetcode.Num78Subsets.Subsets;

import java.util.Arrays;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        Subsets sub = new Subsets();
        int nums[]= {0};
        System.out.println(sub.subsets(nums));
    }
}